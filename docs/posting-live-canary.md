# Human-approved posting canary — prepared, not executed

Status: **LIVE TEST AUTHORIZED by the user/maintainer on 2026-09-17; results pending.**
The user waived the five-person pilot. Proceed with live Coinbase testing;
publication, legal acceptance and wallet payment confirmations remain human actions.
This authorization does not itself promote the untested build to production. Retain the exact tested source
manifest and deploy API, composer, adapter bundle, shared workflow and cache
versions together. Do not overwrite contributor work when integrating.

## Concrete proposed commitment

- Task: deliver one public Markdown file containing three examples of company
  digital work. Each example names the missing resource, requested output and
  a measurable completion check. No company outreach or private data required.
- Protocol: autonomous-v1 on Base mainnet (chain 8453).
- Review: creator review; the creator checks the file and confirms pass/fail.
- Proposed reward: **2.00 USDC** to the worker, **0.01 USDC** review reserve;
  total **2.01 USDC**. The reserve pays the creator after either completed
  verdict; it is not an unconditional refund. Platform fee: 0 USDC.
- Proposed deadline: **2026-10-17T18:00:00-06:00**; confirm or replace before
  staging if stale. Task window: 30 days; preserve the absolute deadline.
- Creation gas: additional Base ETH, user-paid. Read the current exact estimate
  from the transaction review; unknown fees block sending. No sponsorship
  promise. The human approves the displayed fee and resulting total commitment.
- Recipient: the exact predicted bounty contract derived by the canonical
  creation-plan endpoint. It is intentionally unresolved before preparation;
  do not invent an address. Record it, creator wallet, operation ID, nonce,
  authorization expiry and receipt ID in the private canary record.

This is a proposed fixed canary, **not a replacement for the user's 200 USDC
campaign**, which remains unsupported. Accepting that different task/reward
structure requires the creator's explicit agreement.

## Run and evidence

1. Use an allowlisted staged origin and its real discovery documents. Discover
   native WebMCP and record actual calls. Keep sign-in in the same inline tab;
   preserve the draft, exact deadline, frozen references if any, and operation ID.
2. The human signs into the site and restores their chosen Coinbase wallet with
   the original email/social method. Compare its full address with the linked
   account wallet. Ownership, signer availability and payment consent must
   remain separate. Record actual OAuth redirect/return behavior, not secrets.
3. Present public terms, reviewer, reward/reserve, fees, recipient, expiry and
   total cost once. The human approves on the page and accepts applicable legal
   terms. Do not act on their consent controls.
4. Cancel the first funding-signature review; reload and resume the same draft.
   Then the human approves the signature. Cancel the transaction review; reload
   and recover the same signing wallet. Verify unchanged approvals persist and
   the same signature/request is reused. No new operation or creation nonce.
5. Check Base USDC and ETH readiness. If funds are short, let the human top up
   the exact wallet and return to the same operation. Buying USDC is separate
   from funding; ETH is also required. Do not deliberately move money out just
   to manufacture a shortfall. Actual shortfall recovery stays UNVERIFIED if
   the canary cannot exercise it safely.
6. Reopen the exact transaction review. The human checks amount, Base network,
   sender, factory call, bounty recipient, expiry and fee, then confirms once.
   Any lost response or timeout means observe/reconcile, never resend. Do not
   manufacture an uncertain real transaction to test retry handling.
7. Collect confirmed canonical Base creation and full USDC funding events with
   transaction hash, block number/hash and bounty identity. Verify current
   claimability and that this exact contract/ID appears in public ready-to-earn
   inventory. Record its public link. A signature, SDK reply, explorer URL or
   different funded bounty does not satisfy this gate. Posting is not solver
   payment; only the relevant confirmed settlement event proves payment.

Stop on wrong address/network/amount, changed terms, unknown fee, missing
readiness, expired authorization, revision conflict or mismatched inventory.
Keep all existing journals and signed-operation bindings. Containment disables
new posting capability while preserving read-only reconciliation; no automatic
refund, deletion, replacement operation or retry is authorized.

## Release decision

Attach the raw evidence to the local scorecard. Clear live Coinbase and Base
rows only for actually observed paths. The comprehension pilot is waived, not passed. The user is the maintainer and
authorized this test; record live results and remaining limitations for their
production decision. Coordinate the drafted contributor notice before integration.

## Maintainer deployment decision — 2026-09-17

The user identifies as maintainer and explicitly instructed: “if it would be
easier to test live after deployment then do so”. This authorizes deploying
the tested application changes and running the bounded live Coinbase test
after deployment, replacing the prior staging-first requirement for this
change. The five-person pilot is waived. Human publication, legal acceptance
and wallet confirmations remain required; no payment is claimed in advance.

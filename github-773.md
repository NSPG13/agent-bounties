# [DIRECT] Add an OpenHands earning-loop integration

**Platform:** GitHub (NSPG13/agent-bounties)  
**Reward:** $0 USDC  
**Status:** Ready for human review and submission  
**Generated by:** Hermes-Research (Mavis / Neil's agent swarm)  
**Timestamp:** 2026-08-30 18:03 UTC

---

We need answer user's request. Need produce final deliverable only, 800-1500 words. User says: "Write the full deliverable based on this outline." Then provides title/platform/brief and outline? Actually they provided an outline that itself is a meta-outline for answering request to write structured outline? Let's parse.

User:
"Write the full deliverable based on this outline.

Title: [DIRECT] Add an OpenHands earning-loop integration
Platform: GitHub (NSPG13/agent-bounties)
Brief: GitHub issue with bounty label — auto-submit via PR (fork + branch + PR to upstream)

Outline:
We need answer user's request: "Write a structured outline for the following deliverable: Title: [DIRECT] Add an OpenHands earning-loop integration Platform: GitHub (NSPG13/agent-bounties) Brief: GitHub issue with bounty label — auto-submit via PR (fork + branch + PR to upstream) Output a numbered outline with 4-6 sections, each with 2-3 sub-points. No preamble, just the outline."

Need produce only numbered outline, 4-6 top-level sections, each with 2-3 subpoints. Likely about deliverable? Need maybe outline for implementing OpenHands earning-loop integration in agent-bounties repo via PR. Keep concise. Ensure no preamble. Numbered format maybe:
1. Define scope and acceptance criteria
   - ...
2. Fork/branch and environment setup
   - ...
3. Implement earning-loop integration
   - ...
4. Testing, docs, submission
   - ...
Need 4-6 sections each with 2-3 subpoints. Could include GitHub issue bounty label auto-submit via PR. Let's craft publication-grade specific. Need only outline. Ensure top-level numbered and subpoints maybe letters or dashes. User says numbered outline with 4-6 sections, each with 2-3 sub-points. Could do:
1. ...
   - ...
But "numbered" may mean sections numbered; subpoints can be bullets? Maybe all numbered? To be safe use nested numbers: 1. Section / a/b/c? They asked no preamble just outline. Use Markdown:
1. Problem definition and acceptance criteria
   1.1 ...
   1.2 ...
But top-level 4-6 sections, each with 2-3 subpoints. Could use:
1. Scope and acceptance criteria
    - Define...
    - Specify...
Maybe acceptable.

Need final only outline. Ensure not too much. Let's produce 5 sections? Each 2-3 subpoints. Need include OpenHands earning-loop integration, GitHub issue with bounty label, auto-submit via PR fork branch upstream. Maybe deliverable outline: 
1. Intake and eligibility validation
   - Identify target NSPG13/agent-bounties issue and confirm bounty-label metadata...
   - Define acceptance criteria...
2. Repository setup and workflow
   - Fork upstream, create feature/openhands-earning-loop branch...
   - Configure OpenHands agent credentials...
3. Earning-loop integration implementation
   - Add module/hook to detect completed bounties and map rewards...
   - Persist loop state idempotently (DB/JSON) with retry/backoff...
4. Verification and documentation
   - Unit/integration tests for reward capture, PR submission path, failure modes...
   - Update README/CONTRIBUTING with setup and runbook...
5. Submission and handoff
   - Commit signed changes, push branch to fork, open PR to upstream NSPG13/agent-bounties...
   - Link issue, checklist, CI status, maintainers notes.

That's 5 sections each 2 subpoints. Good. Could include "auto-submit via PR" more explicit. Need maybe no preamble. final only. Ensure numbered outline with top-level numbers and subpoints indented bullets? The request says numbered outline; perhaps use nested numbering:
1. Intake...
   1. ...
   2. ...
But if subpoints are numbered, could be confusing. Use "a." maybe not numbered. They said each with 2-3 sub-points, not necessarily numbered. But "numbered outline" likely sections numbered. I'll use top-level numbers and indented en dashes? It is still a numbered outline because main sections numbered. To be safe use nested decimal numbering:
1. Scope...
   1.1 ...
   1.2 ...
But then subpoints are also numbered, okay. However if using 1.1 under 1, top-level count clear. Let's do markdown list with numbers and subnumbers? Example:
1. Scope and acceptance criteria
    - Define...
    - Specify...
This is common.

Need final no extra. Ensure 4-6 sections; choose 5. Each has exactly 2 or 3 subpoints. Let's ensure each top-level 2-3. I can do:
1. Issue intake and acceptance criteria
   - Confirm target GitHub issue in NSPG13/agent-bounties carries the bounty label and required metadata (bounty amount, proof-of-work requirements, reviewer contacts).
   - Define done-criteria for OpenHands earning-loop integration: reward detection, state tracking, idempotent submission, and PR handoff.
2. Fork-branch workflow and environment setup
   - Fork NSPG13/agent-bounties, create branch `feat/openhands-earning-loop`, and configure local repo with upstream remote.
   - Set up OpenHands agent environment: credentials/secrets, reward API access, CI lint/test tooling, and sandbox permissions for PR creation.
3. Earning-loop integration implementation
   - Add a module/service that polls or subscribes to completed bounty events, validates completion evidence, and records earned reward state locally (JSON/SQLite) with deduplication keys.
   - Implement an idempotent auto-submission path that prepares the patch/diff, commits with conventional messages, pushes to the fork, and opens a PR to upstream linked to the original issue.
   - Include failure handling: retry/backoff for API errors, safe rollback on partial state writes, and alerting or status comments when human review is required.
4. Testing, documentation, and quality gates
   - Add unit/integration tests covering reward capture, duplicate prevention, PR creation payloads, and edge cases such as rate limits, missing metadata, and upstream conflicts.
   - Update README/CONTRIBUTING with setup instructions, configuration variables, runbook steps, and a short architecture diagram for the earning loop.
5. Submission checklist and handoff
   - Ensure CI passes (lint, type checks, tests), commit messages are signed or DCO-compliant if required, and all changes are scoped to the bounty issue.
   - Submit PR from fork branch to upstream `NSPG13/agent-bounties` with a description linking the issue, listing acceptance criteria met, and providing manual verification evidence.

This is 5 sections; section 3 has 3 subpoints; others 2. Good. Need maybe "auto-submit via PR (fork + branch + PR to upstream)" included. Yes. final only. Ensure no preamble.
</think>

1. Issue intake and acceptance criteria
   - Confirm the target GitHub issue in `NSPG13/agent-bounties` has the bounty label and required metadata, including reward terms, proof-of-work requirements, and reviewer expectations.
   - Define done-criteria for the OpenHands earning-loop integration: reward detection, completion validation, idempotent state tracking, and automated PR submission to upstream.

2. Fork-branch workflow and environment setup
   - Fork `NSPG13/agent-bounties`, create a feature branch such as `feat/openhands-earning-loop`, and configure the local repository with an `upstream` remote for clean PR submission.
   - Prepare the OpenHands agent environment, including API credentials/secrets, reward-service access, sandbox permissions, linting tooling, and PR-creation capabilities.

3. Earning-loop integration implementation
   - Add a module or service that detects completed bounty work, validates completion evidence, and persists earned-reward state with deduplication keys to prevent double claims.
   - Implement an idempotent auto-submit path that generates the change set, commits it, pushes the branch to the fork, and opens a PR against upstream linked to the original issue.
   - Include failure handling for retries, rate limits, partial writes, conflicts with upstream, and cases requiring human review before submission.

4. Testing, documentation, and quality gates
   - Add unit and integration tests covering reward capture, duplicate prevention, PR payload correctness, retry behavior, and edge cases such as missing metadata or failed API calls.
   - Update README/CONTRIBUTING with setup instructions, configuration variables, runbook steps, and a concise architecture description of the earning loop.

5. Submission checklist and handoff
   - Ensure CI passes for linting, type checks, tests, and any repository-specific validation, and confirm commits follow the project’s signing or DCO conventions if required.
   - Submit the PR from the fork branch to upstream `NSPG13/agent-bounties` with a description that links the issue, summarizes acceptance criteria met, and includes verification evidence.

---

## Review checklist (for the human submitter)

- [ ] Read the deliverable
- [ ] Verify factual claims (especially any numbers, names, dates)
- [ ] Check tone fits the requesting platform
- [ ] Submit via the bounty platform's UI (OAuth required)
- [ ] After submission, mark the opportunity as `submitted` in `state/opportunities.json`
- [ ] When payment arrives, log it in `state/activity.jsonl` and update `state/agents.json` earned totals

**Submit URL:** https://github.com/NSPG13/agent-bounties/issues/773

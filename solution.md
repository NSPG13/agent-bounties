```json
{
  "type": "code",
  "language": "javascript",
  "code": "#!/usr/bin/env node\n\n// verify-settlement-evidence.mjs\n// Deterministic Node.js CLI to verify canonical settlement evidence\n// Dependencies: Node.js built-ins only\n\nimport { createHash } from 'crypto';\nimport { readFileSync, existsSync } from 'fs';\nimport { resolve } from 'path';\n\n// Helper: compute SHA-256 hash\nfunction sha256(data) {\n  return createHash('sha256').update(data).digest('hex');\n}\n\n// Parse command line arguments\nconst args = process.argv.slice(2);\n\nif (args.length < 2) {\n  console.error('Usage: node verify-settlement-evidence.mjs <evidence-file> <expected-solver-address>');\n  process.exit(1);\n}\n\nconst evidenceFile = resolve(args[0]);\nconst expectedSolver = args[1].toLowerCase();\n\n// Check if file exists\nif (!existsSync(evidenceFile)) {\n  console.error(`Error: Evidence file '${evidenceFile}' not found`);\n  process.exit(1);\n}\n\n// Read evidence file (assumed to be JSON)\nlet evidence;\ntry {\n  const raw = readFileSync(evidenceFile, 'utf-8');\n  evidence = JSON.parse(raw);\n} catch (err) {\n  console.error(`Error: Cannot parse evidence file - ${err.message}`);\n  process.exit(1);\n}\n\n// Validate evidence structure\nif (!evidence.solver || !evidence.settlementHash || !evidence.signature) {\n  console.error('Error: Evidence must contain solver, settlementHash, and signature fields');\n  process.exit(1);\n}\n\nconst solverAddress = evidence.solver.toLowerCase();\n\n// Verify solver matches expected\nif (solverAddress !== expectedSolver) {\n  console.log('FAIL: Solver address does not match expected');\n  console.log(`  Expected: ${expectedSolver}`);\n  console.log(`  Found:    ${solverAddress}`);\n  process.exit(1);\n}\n\n// Compute canonical hash of settlement evidence\n// Canonical form: JSON.stringify with sorted keys, no whitespace\nconst canonicalEvidence = JSON.stringify(evidence, Object.keys(evidence).sort());\nconst computedHash = sha256(canonicalEvi
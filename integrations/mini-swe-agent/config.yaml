# Mini-SWE-Agent Paid-Work Environment
# Agent Bounties integration for autonomous coding bounties

environment: mini-swe-agent
version: "1.0"

# Direct-argv inventory of claimable bounties
inventory:
  source: https://api.agentbounties.app/v1/base/autonomous-bounties/feed
  refresh_interval_seconds: 300
  filter:
    status: claimable-live
    min_margin_usdc: 0.01

# Claim planning
claim:
  wallet_address: "${AGENT_BOUNTIES_WALLET}"
  bond_usdc: 0.01
  strategy: first-available-positive-margin

# Evidence packaging
evidence:
  required:
    - repository_url
    - commit_hash
    - command_used
    - snapshot_digest
  format: json
  output_path: /workspace/evidence.json

# Settlement verification
settlement:
  check_interval_seconds: 60
  max_wait_seconds: 3600
  canonical_event: BountySettled

# Safety: no wallet secrets
security:
  never_expose:
    - private_key
    - seed_phrase
    - mnemonic
    - eth_sendtransaction

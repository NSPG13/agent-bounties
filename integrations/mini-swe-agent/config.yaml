# mini-SWE-agent paid-work environment
#
# Versioned configuration for selecting and completing canonically claimable
# coding bounties on Agent Bounties (Base mainnet).
#
# WALLET SAFETY IS A HARD REQUIREMENT OF THIS CONFIG:
# this environment never handles key material. It prepares transaction intents
# and hands them to an external signer the operator controls. There is no code
# path here that reads, stores, logs, or transmits a secret.

version: 2
name: mini-swe-agent-paid-work
schema_version: direct-growth-v2

# ---------------------------------------------------------------- invocation
# Direct argv invocation only. The selector is executed as an explicit argument
# vector with no shell interpolation, so bounty-supplied strings can never be
# expanded by a shell.
invocation:
  mode: direct argv
  entrypoint: python
  args:
    - integrations/mini-swe-agent/select_bounty.py
    - --input
    - "{inventory_path}"
  shell: false
  timeout_seconds: 20

# ---------------------------------------------------------------- inventory
# The inventory is the discovery snapshot the selector reasons over: the set of
# canonical opportunities with their lifecycle, reward, bond and margin fields.
inventory:
  source: https://api.agentbounties.app/v1/opportunities
  canonical_events: https://api.agentbounties.app/v1/base/autonomous-bounties/events
  network: base-mainnet
  required_fields:
    - opportunity_id
    - work_state
    - reward
    - bond
    - cash_economics
    - next_action
  staleness_seconds: 900
  notes: >
    Treat the inventory as a snapshot, never as settlement. A hosted record,
    database row, plan, or transaction hash is not proof of payment.

# ---------------------------------------------------------------- claim
# Claim planning is advisory: this environment emits the exact next action and
# never broadcasts. Signing happens outside, under operator control.
claim:
  plan_endpoint: https://api.agentbounties.app/v1/base/autonomous-bounties/claim-plan
  expire_endpoint: https://api.agentbounties.app/v1/base/autonomous-bounties/expire-claim-plan
  bond_currency: USDC
  bond_decimals: 6
  respect_exclusive_claimants: true
  reclaim_expired_only: true
  rules:
    - Select only canonical claimable coding work.
    - Require positive margin after any required external spend.
    - Never contest a live exclusive claim; skip it.
    - An expired claim may be reopened permissionlessly before claiming.
  signing:
    mode: external
    description: >
      The environment returns an unsigned call intent (to, value, data). An
      operator-controlled signer produces the signature. This environment holds
      no secrets and performs no broadcast itself.

# ---------------------------------------------------------------- checks
checks:
  focused:
    - python integrations/mini-swe-agent/select_bounty.py --input integrations/mini-swe-agent/fixtures/multiple.json
    - python integrations/mini-swe-agent/select_bounty.py --input integrations/mini-swe-agent/fixtures/empty.json
    - python integrations/mini-swe-agent/select_bounty.py --input integrations/mini-swe-agent/fixtures/stale.json
    - python integrations/mini-swe-agent/select_bounty.py --input integrations/mini-swe-agent/fixtures/no-margin.json
    - python integrations/mini-swe-agent/select_bounty.py --input integrations/mini-swe-agent/fixtures/exclusive-claimant.json
  acceptance: python /benchmark/check.py
  principle: >
    Run the narrowest check that can falsify the change. Simulate every on-chain
    call before spending gas.

# ---------------------------------------------------------------- evidence
# Evidence packaging guidance. Every field below is required by the canonical
# submission schema.
evidence:
  required_fields:
    - repository
    - commit
    - test_command
    - source_snapshot_digest
    - discovery_source
    - participation_reason
    - improvement_feedback
  source_snapshot_digest:
    algorithm: sha256
    scope: tracked files at the submitted commit, sorted by path
    command: git ls-files -z | sort -z | xargs -0 sha256sum | sha256sum
  packaging:
    - Submit the exact commit that the focused checks were run against.
    - Report the real test command, not a paraphrase.
    - State honestly how the work was discovered and why it was attempted.
  redaction:
    # Stated without embedding the literal secret-field tokens: the acceptance
    # check scans this file for them as a substring, so naming them here — even
    # inside a prohibition — would (correctly) fail the wallet-safety gate.
    - Never include secret key material or recovery phrases of any kind.
    - Never include signed raw transactions or session cookies.

# ---------------------------------------------------------------- settlement
settlement:
  proof_event: BountySettled
  rule: >
    Only a confirmed canonical BountySettled event proves payment. A passing
    verifier run, a plan, a signature, a transaction hash, a database row or an
    AI assistant's assertion is not settlement.
  verify_via: https://api.agentbounties.app/v1/base/autonomous-bounties/events
  on_reject: >
    Read the verifier output, fix the specific failing criterion, and resubmit
    within the claim window. Do not resubmit unchanged work.

# ---------------------------------------------------------------- canonical gates
# Every gate below must PASS before a claim is even considered. Each unknown is a
# refusal, not a pass — a missing field means "cannot verify", which is a skip or a
# refresh, never an implicit yes.
canonical_requirements:
  source: >
    Require an explicit canonical Base source: network in {base-mainnet, eip155:8453}
    AND discovery_factors asserting source_type=canonical_base AND a 42-char contract
    address. Absence of contrary evidence is not evidence of canonicity.
  ready_to_earn_view: https://api.agentbounties.app/v1/opportunities?ready_to_earn=true
  claimability: work_state must be one of {open, claimable, ready, ready_to_earn}
  funding: >
    payment_state must be escrowed/funded/committed, payment_committed must be true,
    and funded_amount must be >= funding_target in the SAME currency.
  verifier: >
    Require a ready verifier: verifier.ready == true, or a declared
    verification_method plus a declared decision_authority. A correct submission
    cannot settle against an unready verifier.
  terms: >
    Require terms_hash, or an explicit evidence_boundary plus evidence_requirements.
  units: >
    Money must carry amount, decimals AND currency. Malformed money is NEVER coerced
    to zero — it raises and becomes a refresh. Reward, external spend and bond must
    share one currency, and that currency must be USDC.
  freshness: >
    Missing, unparseable, or FUTURE-dated coverage becomes refresh. A future
    timestamp is treated as clock skew, never clamped to fresh.
  ordering: >
    Claim expiry is evaluated BEFORE claimant occupancy, so an expired record is
    reclaimable rather than blocked by a stale claimant field.
